# Client-side (bundled into JS, used by browser for e.g. lead form submit)
NEXT_PUBLIC_PB_URL=https://pb.kuiper-safety.de
# Server-side (used by SSR/ISR — bypasses Cloudflare Access via internal Docker network)
PB_INTERNAL_URL=http://pocketbase-tcbnib1hhrx3qtw0kjo9neqo-PLACEHOLDER:8090
